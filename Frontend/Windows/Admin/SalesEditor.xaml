<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Frontend.Windows.Admin"
        xmlns:gif="https://github.com/XamlAnimatedGif/XamlAnimatedGif"
        xmlns:Collections="clr-namespace:System.Collections;assembly=System.Runtime" x:Class="ShoeStore.Frontend.Windows.Admin.SalesEditor"
        mc:Ignorable="d"
        Title="Редактор продаж" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Button x:Name="backToPanelButton" Content="Назад" HorizontalAlignment="Right" VerticalAlignment="Top" Height="40" Click="BackToPanelButton_Click" Width="40" Margin="0,10,10,0"/>
        <Grid Margin="10,58,10,16">
            <DataGrid AutoGenerateColumns="False" x:Name="dataGrid" CanUserAddRows="False" RowEditEnding="DataGrid_RowEditEnding">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Название товара"  Binding="{Binding ItemTitle}" Width="*" />
                    <DataGridTextColumn Header="Логин сотрудника"   Binding="{Binding EmployeeLogin}"  Width="*" />
                    <DataGridTextColumn Header="Дата и время" Binding="{Binding CreatedAt}" Width="*" IsReadOnly="True"/>
                    <DataGridCheckBoxColumn Header="Вернули назад" Binding="{Binding IsReturned}" Width="*" />
                    <DataGridTemplateColumn Header="">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button x:Name="returnItem" Content="Оформить возврат" Click="ReturnItem_Click"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        <Grid x:Name="LoadingGrid" Visibility="Hidden">
            <Rectangle Height="414" Stroke="Black" Width="780" Fill="#7F000000"/>
            <Image gif:AnimationBehavior.RepeatBehavior="Forever" gif:AnimationBehavior.SourceUri="/Loading2.gif" Source="/Loading2.gif" Width="100" Height="100" />
        </Grid>
        <Button x:Name="addSaleButton" Content="Добавить продажу" HorizontalAlignment="Right" VerticalAlignment="Top" Height="40" Width="125" Margin="0,10,55,0" Click="AddSaleButton_Click"/>
    </Grid>
</Window>
